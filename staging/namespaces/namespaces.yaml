---
# Tenant Namespaces will be instantiated by the
# GitOps reconciler which is the Tenant Owner.
#
apiVersion: v1
kind: Namespace
metadata:
  labels:
    toolkit.fluxcd.io/tenant: dev-team
    kubernetes.io/metadata.name: tools
  name: tools

---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    toolkit.fluxcd.io/tenant: dev-team
    kubernetes.io/metadata.name: apps
  name: apps
